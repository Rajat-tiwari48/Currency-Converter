import InputBox from "./InputBox";


//we use this because all components are imported here and all are exported with ite same with use of , so it
//decrease the complexity of the program and easy the understand 
// because there will be a single file through which all componrnts can be accessed
// and for accessing also we need to import only one file and can access all components
export{InputBox}